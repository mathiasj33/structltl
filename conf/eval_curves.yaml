defaults:
  - experiment: ltl2action/zones
  - hydra: simple
  - _self_

run: newltl2action
env:
  use_precomputed_resets: true
  precomputed_resets_path: sampled_resets_test.eqx
eval:
  num_episodes: 16
  deterministic: false
  discount: 0.998
  models_per_batch: all
  formulas_per_batch: all
