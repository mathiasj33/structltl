defaults:
  - experiment: struct_ltl/zones_nm
  - hydra: simple
  - _self_

run: main
env:
  use_precomputed_resets: true
  precomputed_resets_path: sampled_resets_test.eqx
eval:
  finite: true
  num_episodes: 50
  deterministic: false
  discount: 0.998
  models_per_batch: 5
  formulas_per_batch: all
