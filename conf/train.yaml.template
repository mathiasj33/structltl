defaults:
  - experiment: struct_ltl/zones_nm
  - hydra: training_run
  - _self_

run: tmp
num_seeds: 10
start_seed: 0
use_gpu: true # use gpu if available
save_freq: 2e6

wandb:
  project: project-name
  use_wandb: false
